# BeatFlower Library and Demo program

Build it like so:

```bash
mkdir build
cd build
cmake ..
make
```

Run it like to:

```bash
cd bin
./BeatFlower-demo
```

If it doesn't work, adjust the `ttyUSB<n>` port in the `demo.cpp`
source file.